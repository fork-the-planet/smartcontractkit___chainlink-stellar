# Chainlink CCIP Stellar

## Contracts Commands

The `./Makefile` in the home directory has a few helper commands.

* ...
* `generate-interfaces`: auto generates the interfaces for each contract to be used for cross-contract calls.