[package]
name = "common-error"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["lib"]
doctest = false

[dependencies]
common-error-derive = { path = "../error-derive" }

[dev-dependencies]
